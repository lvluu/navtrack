{
  "name": "@navtrack/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "vite --host 0.0.0.0 --no-open",
    "build": "vite build",
    "serve": "vite --no-open preview",
    "cosmos": "cosmos",
    "cosmos:export": "cosmos-export",
    "watch": "tsc --noEmit --watch"
  },
  "browserslist": [
    "defaults"
  ]
}
